@model IEnumerable<backenddb_c.Models.Pago>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MontoPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodigoPrestamoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MontoPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoPrestamoNavigation.CodigoPrestamo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NumeroPago">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NumeroPago">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NumeroPago">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
